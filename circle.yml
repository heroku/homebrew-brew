machine:
  xcode:
    version: 9.1

dependencies:
  cache_directories:
    - ~/Library/Caches/Homebrew
    - /usr/local/Homebrew/Library/Taps/caskroom
    - /usr/local/Homebrew/Library/Taps/homebrew
  pre:
    - brew update
    - brew cleanup

test:
  override:
    - ./scripts/test
